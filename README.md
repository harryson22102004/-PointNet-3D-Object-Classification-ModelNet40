# PointNet++ — 3D Object Classification ModelNet40
Hierarchical point-cloud deep learning for 3D object classification.
### ■ Research Highlights
- **Set Abstraction Layers**: Recursively applies furthest-point sampling, ball-query grouping, and PointNet - **Multi-Scale Grouping**: Applies MSG (multi-scale grouping) at each layer to handle non-uniform point den- **Algorithms Focus**: Reports overall accuracy and per-class mean accuracy on ModelNet40, comparing PointNe**Tech Stack:** Python, `PyTorch`, `PyTorch Geometric`.
